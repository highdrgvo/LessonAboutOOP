package ru.serple;

import ru.serple.data.Card;

public class Main {
    public static void main(String[] args) {

        Card masterCard = new Card(); // Создание объекта (экземпляра) класса Card. masterCard - это переменная, которая будет хранить ссылку на объект. new Card() — создает новый объект (экземпляр) класса Card в памяти.
    }
}